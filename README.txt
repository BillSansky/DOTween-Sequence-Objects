Thanks for downloading :)

In order for this to work, you will need Odin Inspector, and a DOTween Pro version that allows manual update (12-08-2018 currently in preview).
Those scripts also use .NET 4.x so you'll also need a recent version of Unity (or you can always convert those scripts)